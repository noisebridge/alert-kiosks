all:
  children:
    kiosks:
      hosts:
        charge-alerts:
          ansible_host: charge-alerts.local
        charge-alerts2:
          ansible_host: charge-alerts2.local
        charge-alerts3:
          ansible_host: charge-alerts3.local
