all:
  children:
    charge_kiosks:
      hosts:
        charge-alerts1:
          ansible_ssh_private_key_file: ~/.ssh/charge-alerts
        charge-alerts2:
          ansible_ssh_private_key_file: ~/.ssh/charge-alerts
        charge-alerts3:
          ansible_ssh_private_key_file: ~/.ssh/charge-alerts
        charge-alerts4:
          ansible_ssh_private_key_file: ~/.ssh/charge-alerts
    membership_kiosks:
      hosts:
        membership-alerts:
          ansible_ssh_private_key_file: ~/.ssh/membership-alerts
